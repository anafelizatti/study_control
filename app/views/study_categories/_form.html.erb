<%= form_with(model: study_category, local: true) do |form| %>
  <% if study_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(study_category.errors.count, "error") %> prohibited this study_category from being saved:</h2>

      <ul>
        <% study_category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label "Categoria" %>
    <br>
    <%= form.text_field :category %>
  </div>

  <div class="actions">
    <%= form.submit "Nova categoria" %>
  </div>
<% end %>
